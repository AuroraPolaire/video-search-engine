{
  "name": "video-search-engine",
  "source": "src/index.html",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "scripts": {
    "start": "parcel ./*.html",
    "predeploy": "rm -rf dist && parcel build ./*.html --public-url https://github.com/AuroraPolaire/video-search-engine",
    "deploy": "gh-pages -d dist"
},
  
  "devDependencies": {
    "@parcel/transformer-sass": "^2.9.1",
    "gh-pages": "^5.0.0",
    "parcel": "^2.9.1",
    "shx": "^0.3.2"
  }
}
